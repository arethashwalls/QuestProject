import React from 'react';
import SigninForm from '../../components/SigninForm';

const Signin = () => {
    return <div>
        <h1>Welcome back to QuestApp!</h1>
        <SigninForm />
    </div>
}

export default Signin;